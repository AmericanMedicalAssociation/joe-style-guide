<div class="joe__tags">
  <ul class="joe__tags-list">
    {% for tag in tags %}
      {% if loop.last %}
        <li class="joe__tag"><a href="{{tag.url}}">{{tag.text}}</a></li>
      {% else %}
        <li class="joe__tag"><a href="{{tag.url}}">{{tag.text}}</a>,</li>
      {% endif %}
    {% endfor %}
  </ul>
</div>
