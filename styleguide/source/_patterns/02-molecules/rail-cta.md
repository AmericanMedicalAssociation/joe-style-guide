### Description
This Pattern shows the rail call to action.

### Variables
~~~
rail_cta {
  heading.text:
    type: string / required
  paragraph.text:
    type: string / required
  button.text:
    type: string / required
  button.url:
    type: string (url) / required
}
~~~
